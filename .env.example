# Cloudflare Tunnel token (get from Zero Trust dashboard)
# Create tunnel: https://one.dash.cloudflare.com → Networks → Tunnels → Create
# Set public hostname to point to http://frontend:80
TUNNEL_TOKEN=your_tunnel_token_here
